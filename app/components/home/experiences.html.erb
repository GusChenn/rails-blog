<div class='flex flex-col gap-2 px-8 mb-8 overflow-hidden'>
  <% experiences.each do |experience| %>
    <%= render(Home::ExperienceCard.new(experience)) %>
  <% end %>

  <div class="absolute top-full left-1/4 w-fuller h-full bg-gradient-to-t from-indigo-400 to-transparent origin-top-left -rotate-30 z-0"></div>
</div>
